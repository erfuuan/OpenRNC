import auth from './auth';
import consume from './consume';
import Destination from './destination';
import source from './source';
import pipeline from './pipeline';

export default { auth, source, Destination, pipeline, consume };
